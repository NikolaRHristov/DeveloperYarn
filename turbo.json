{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "prepublishOnly": {
      "dependsOn": [
        "^prepublishOnly"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*"
      ],
      "outputs": [
        ".next/**",
        "!.next/cache/**"
      ]
    }
  }
}
